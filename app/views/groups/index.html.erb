<h1> My Groups</h1>
<% if current_user.has_role? :admin %>
    <%= link_to 'New Group', new_group_path %>
<% end %>
<%= link_to 'Find a group', find_group_path %>
<% @groups.each do |group| %>
<%= group.name %>
<% end %>